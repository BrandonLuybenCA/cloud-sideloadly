name: 0. Fetch My UDID
on:
  workflow_dispatch:

jobs:
  discovery:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y libimobiledevice-utils netmuxd strongswan jq
          
      - name: Start VPN
        run: sudo bash scripts/setup_vpn.sh

      - name: Start Ngrok
        run: |
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok
          ngrok config add-authtoken 3F04f0qQaW68FVs9RAmoV0urkni_4ss21AMnPXEEEZq6GZw6t
          ngrok tcp 500 --log=stdout > ngrok.log &
          sleep 10
          NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
          echo "--------------------------------------------------"
          echo "VPN ADDRESS: $NGROK_URL"
          echo "--------------------------------------------------"

      - name: Discovery
        run: |
          for i in {1..5}; do
            sudo netmuxd --host 10.10.10.$i &
          done
          python3 scripts/discovery.py
